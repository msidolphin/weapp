<i-cell-group>
  <i-cell wx:for="{{list}}" title="选项-{{index + 1}}" wx:key="{{index}}" data-index="{{index}}" bindtap="onOpen" is-link value="{{item.text}}"></i-cell>
</i-cell-group>

<i-mutex-select id="text-select"></i-mutex-select>