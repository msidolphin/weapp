<view wx:for="{{100}}" wx:key="{{item}}">{{item}}</view>
<i-bottom-drawer id="drawer">
  <view slot="header" class="title">招聘岗位</view>
  <view style='padding:20px;'>
    <view wx:for="{{200}}" wx:key="{{item}}">列表项{{item + 1}}</view>
  </view>
</i-bottom-drawer>